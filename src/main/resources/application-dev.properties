#CREDENTIALS DATABASE
spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USER}
spring.datasource.password=${DB_PASSWORD}

#FRONT-END DOMAIN (CORS)
app.cors.allowed-origin=http://localhost:8080

#PORT API
server.port=8081